import Layout from "../layout";
import { NoData } from '../../../utils/errorResponse'

function Settings() {
    return (
        <Layout>
            <NoData />
        </Layout>
    );
}

export default Settings;
